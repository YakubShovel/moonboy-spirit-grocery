---
layout: base.njk
---
<article class="post{% if poem %} poem{% endif %}">
  <header class="post-header">
    <h1>{{ title }}</h1>
    <time datetime="{{ date | htmlDateString }}">{{ date | readableDate }}</time>
  </header>
  <div class="post-body">
    {{ content | safe }}
  </div>
</article>
